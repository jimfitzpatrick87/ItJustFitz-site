# =============================================================================
# LIVE - the site is open to search engines as of 2026-08-16.
#
# The pre-launch block that used to live here was lifted once the lead path was
# proven end to end: a real submission reached Cloudflare D1, a Resend
# notification arrived at itjustfitzai@gmail.com from notifications@itjustfitz.com,
# and the linked record rendered behind Cloudflare Access.
#
# The old blocker text referenced a Supabase anon key. That dependency is gone -
# index.html now posts same-origin to /api/inquiry and reads no key at all.
#
# ---------------------------------------------------------------------------
# READ THIS BEFORE EDITING: Cloudflare prepends its own block above this file.
#
# Cloudflare's Managed robots.txt injects a "BEGIN Cloudflare Managed content"
# section at the edge, ahead of everything below. That section already contains
# its own "User-agent: *" group, so the served file has TWO groups for "*".
#
# Google merges duplicate groups and resolves conflicts least-restrictive-first,
# which means an "Allow: /" up there quietly overrides a "Disallow: /" down here.
# Other crawlers do not all agree on that. Never rely on the interaction:
#
#   - Check the SERVED file, not this one: curl https://itjustfitz.com/robots.txt
#   - If this file and the managed block ever disagree, fix it in the Cloudflare
#     dashboard (Websites -> itjustfitz.com -> Settings -> Managed robots.txt),
#     not by adding a louder rule here.
#
# The managed block also carries Content-Signal directives and per-bot AI-crawler
# rules (Amazonbot, ClaudeBot, GPTBot, Google-Extended and others are blocked
# there). Those are managed by Cloudflare - do not duplicate them here.
# =============================================================================

User-agent: *
Allow: /

# Admin and client surfaces are never indexed, launched or not.
#
# /admin is additionally gated by Cloudflare Access, and both admin Functions
# return 403 without a Cf-Access-Jwt-Assertion header. This line is hygiene on
# top of those, not the control - robots.txt is a request, not a gate.
Disallow: /admin
Disallow: /dashboard.html
Disallow: /portal.html
Disallow: /assets/archive/

Sitemap: https://itjustfitz.com/sitemap.xml
